# Base URL of the CRM API server (Better Auth + REST).
# The client stores its bearer token in localStorage (crm_bearer_token) and
# sends it as Authorization: Bearer on every request.
VITE_API_URL=http://localhost:3000
